let name="Kajal";//Create a variable name to store your name
let age=21;//Create a variable age to store your age
console.log(name,age);//Print the values stored in the variable on one line followed by the type of the variable in the next line
console.log(typeof(name));//Print the values stored in the variable on one line followed by the type of the variable in the next line
console.log(typeof(age));//Print the values stored in the variable on one line followed by the type of the variable in the next line